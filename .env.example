# Infrastructure Settings
DATABASE_URL=sqlite+aiosqlite:///./data/affiliate.db

# Network & Ports
BACKEND_PORT=8000
FRONTEND_PORT=3005
NEXT_PUBLIC_API_URL=http://localhost:8000

# Note:
# All bot configurations, OpenRouter AI keys, Meta Threads credentials,
# viral thread criteria, and cron scheduling intervals are managed directly
# inside the Web Admin Dashboard (http://localhost:3005/admin).
# No need to edit .env files!
